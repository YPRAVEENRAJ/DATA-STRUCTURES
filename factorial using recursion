#include<stdio.h>
int factorial(int n){
if(n==0){
return 1;
}else{
return n*factorial(n-1);
}
}
int main(){
int num;
int n;
printf("enter a number:");
scanf("%d",&num);
printf("fsctorial of %d is %d\n",n,product);
return 0;
}
